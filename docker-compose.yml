services:
  postgres:
    image: postgres:18-alpine
    ports:
      - "5432:5432"
    environment:
      POSTGRES_DB: cwfgw4k
      POSTGRES_USER: cwfgw4k
      POSTGRES_PASSWORD: cwfgw4k
    volumes:
      - pgdata:/var/lib/postgresql

volumes:
  pgdata:
